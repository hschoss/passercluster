---
apiVersion: v1
kind: Namespace
metadata:
  name: homelab-dns
  labels:
    toolkit.fluxcd.io/tenant: sre-team
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: coredns
  namespace: homelab-dns
spec:
  interval: 24h
  url: https://coredns.github.io/helm
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: coredns
  namespace: homelab-dns
spec:
  interval: 12h
  install:
    strategy:
      name: RetryOnFailure
  upgrade:
    strategy:
      name: RetryOnFailure
  chart:
    spec:
      chart: coredns
      version: ">=1.39.0"
      sourceRef:
        kind: HelmRepository
        name: coredns
        namespace: homelab-dns
  values:
    fullnameOverride: homelab-coredns
    replicaCount: 2
    serviceType: LoadBalancer
    service:
      # Pi-hole should point the passer.lan zone at this IP.
      # Envoy uses the first MetalLB address, so CoreDNS reserves the next address.
      loadBalancerIP: 192.168.178.241
      annotations:
        metallb.universe.tf/address-pool: lan-pool
    servers:
      - zones:
          - zone: .
            use_tcp: true
        port: 53
        plugins:
          - name: errors
          - name: health
            configBlock: |-
              lameduck 5s
          - name: ready
          - name: log
            parameters: . "{remote} {type} {class} {name} {rcode} {duration}"
          - name: prometheus
            parameters: 0.0.0.0:9153
          # ExternalDNS writes .passer.lan records into etcd using the CoreDNS provider.
          - name: etcd
            parameters: passer.lan
            configBlock: |-
              path /skydns
              endpoint http://10.97.78.53:2379
          # Keep general DNS usable from this resolver when clients query non-passer.lan names.
          - name: forward
            parameters: . 1.1.1.1 9.9.9.9
          - name: cache
            parameters: 30
          - name: loop
          - name: reload
          - name: loadbalance
