# Node 202 / 192.168.178.202: OS on /dev/sdb, full NVMe for Longhorn performance tier.
machine:
  install:
    disk: /dev/sdb
  disks:
    - device: /dev/nvme0n1
      partitions:
        - mountpoint: /var/mnt/longhorn-performance
