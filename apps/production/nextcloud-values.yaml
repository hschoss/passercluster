apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  chart:
    spec:
      version: ">=1.0.0"
  values:
    nextcloud:
      host: nextcloud.fritz.box
    persistence:
      size: 20Gi
    mariadb:
      primary:
        persistence:
          size: 5Gi
