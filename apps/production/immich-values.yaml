apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich
spec:
  values:
    defaultPodOptions:
      nodeSelector:
        kubernetes.io/hostname: talos-byo-5ko
    valkey:
      persistence:
        data:
          size: 5Gi
