apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  values:
    nextcloud:
      host: nextcloud-staging.passer.lan
      configs:
        domain.config.php: |-
          <?php
          $CONFIG = array(
            'trusted_domains' => ['nextcloud-staging.passer.lan'],
            'overwrite.cli.url' => 'https://nextcloud-staging.passer.lan',
          );
    persistence:
      size: 4Gi
    livenessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.lan
    readinessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.lan
    startupProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.lan
    mariadb:
      primary:
        persistence:
          size: 1Gi
