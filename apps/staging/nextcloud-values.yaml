apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  values:
    nextcloud:
      host: nextcloud-staging.homelab
      configs:
        domain.config.php: |-
          <?php
          $CONFIG = array(
            'trusted_domains' => ['nextcloud-staging.homelab', 'localhost'],
            'overwrite.cli.url' => 'https://nextcloud-staging.homelab',
          );
    persistence:
      size: 4Gi
    livenessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.homelab
    readinessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.homelab
    startupProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.homelab
    mariadb:
      primary:
        persistence:
          size: 1Gi
