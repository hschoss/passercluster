apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  values:
    nextcloud:
      host: nextcloud-staging.passer.home.arpa
      configs:
        domain.config.php: |-
          <?php
          $CONFIG = array(
            'trusted_domains' => ['nextcloud-staging.passer.home.arpa', .home.arpahost'],
            'overwrite.cli.url' => 'https://nextcloud-staging.passer.home.arpa',
          );
    persistence:
      size: 4Gi
    livenessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.home.arpa
    readinessProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.home.arpa
    startupProbe:
      httpHeaders:
        - name: Host
          value: nextcloud-staging.passer.home.arpa
    mariadb:
      primary:
        persistence:
          size: 1Gi
