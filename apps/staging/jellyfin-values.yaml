apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  chart:
    spec:
      version: ">=1.0.0-alpha"
  values:
    persistence:
      config:
        size: 1Gi
      media:
        size: 5Gi
